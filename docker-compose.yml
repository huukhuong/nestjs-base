services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nestjs-base-backend
    ports:
      - '${PORT}:${PORT}'
    env_file:
      - .env
    restart: unless-stopped
